import { FilterBySubcategoriaPipe } from './filter-by-subcategoria.pipe';

describe('FilterBySubcategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySubcategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
